# assets

Put images here:

- `profile.jpg`: profile photo (square works best), used in the hero section
- `code-economics.jpg`, `next-gen-leaders.jpg`: optional achievement photos

See the `PLACEHOLDER` comments in `index.html` for where to reference them.
